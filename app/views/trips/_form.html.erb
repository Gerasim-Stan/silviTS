<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :hour %><br />
    <%= f.time_select :hour %>
  </div><br />
  <div class="field">
    <%= f.label :starting_point %><br />
    <%= f.text_field :starting_point %>
  </div><br />
  <div class="field">
    <%= f.label :destination %><br />
    <%= f.collection_select :destination_id, Destination.order(:name), :id, :name %>
  </div><br />
  <div class="field">
    <%= f.label :event %><br />
    <%= f.collection_select :event_id, Event.order(:name), :id, :name %>
  </div><br />
  <div class="actions">
    <%= f.submit %>
  </div><br />
<% end %>
